role: monitoring
mutating: false
targets: ["*"]
commands:
  # Monitoring uses HTTP/metrics APIs, not SSH, but a tiny shell escape hatch
  # for tcp probes is needed:
  - id: nc-tcp-probe
    match: ^nc -zv -w 3 [a-zA-Z0-9_.-]+ [0-9]{2,5}$
  - id: ping
    match: ^ping -c 1 -W 2 [a-zA-Z0-9_.-]+$
